import { StyleSheet } from "react-native";
import { colors } from "../../styles/colors";

export const styleNotification = StyleSheet.create({
    fullbox: {
        width: '100%', height: '100%',
    }
});